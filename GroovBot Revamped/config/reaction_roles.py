"""
Format:
reaction_roles = {
    messageId: [
        (emoji, roleId),
        (emoji, roleId)
    ],
    messageId: [
        (emoji, roleId)
    ]
}
"""

reaction_roles = {
    messageid: [
        ("emoji", roleid),#Pink
        ("emoji", roleid)
    ]
}
